paths:
  /api/chat:
    get:
      summary: Muestra todos los mensajes disponibles
      tags:
        - Mensajes
      responses:
        "200":
          description: Todos los mensajes disponibles
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/Product'