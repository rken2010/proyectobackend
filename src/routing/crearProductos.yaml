path:
  /api/productos:
    post:
      summary: Crea y almacena un producto en el catalogo
      tags:
        - Productos
      requestBodies:
        required: true
        content:
          application/json:
            schemas:
              $ref: '#/components/requestBodies/nuevoProducto'
      responses:
        "201":
          description: Producto creado
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Productos'
